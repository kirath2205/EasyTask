version: "3.8"

services:
  redis:
    image: redis:7.2-alpine
    container_name: easytask_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --notify-keyspace-events Ex
    restart: always

volumes:
  redis_data:
